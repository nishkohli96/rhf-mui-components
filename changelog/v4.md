# Changelog - v4

## [4.1.3](https://github.com/nishkohli96/rhf-mui-components/tree/v4.1.3)

**Released — 9 July, 2026**

### 🐞 Bug Fixes
- Added missing exports for `RHFMultiAutocompleteObject` and `RHFMultiAutocompleteObjectProps` to the `mui` index module.
- Fixed the JSDoc for the `showLabelAboveFormField` prop in `RHFCheckboxGroup`.


## [4.1.2](https://github.com/nishkohli96/rhf-mui-components/tree/v4.1.2)

**Released — 8 July, 2026**

### 🐞 Bug Fixes
- Corrected field error resolution to prefer `renderError`, then legacy `errorMessage`, then RHF-derived error messages.


## [4.1.1](https://github.com/nishkohli96/rhf-mui-components/tree/v4.1.1)

**Released — 7 July, 2026**

### ⚙️ Project & Tooling
- Moved internal component/helper types into `@/common/types`, keeping `@/types` focused on public package types only.
- Remove examples link from `v1` and `v2` docs.
- Remove source code links from examples website.
- Upgrade **pnpm** from `11.8.0` to `11.10.0`
- Add `@vercel/analytics` to docs and demo website.

### ✨ Enhancements
- Added package exports for the **types** module so consumers can import from `@nish1896/rhf-mui-components/types` and related type subpaths.
- Update workspace root and package `README.md` with **StackBlitz** example links.
- Added **StackBlitz** playground link to the docs website header.
- Adjust dimensions of appbar logo for both docs and demo website.
- Documented `renderError` in the deprecated `errorMessage` JSDoc as the replacement for custom error rendering.


## [4.1.0](https://github.com/nishkohli96/rhf-mui-components/tree/v4.1.0)

**Released — 5 July, 2026**

### ⚙️ Project & Tooling
- Update `setup.sh` script
- Add `vercel.json`
- Replaced `scripts/setup.sh` usage in GitHub workflows with explicit CI steps.
- **RHF Docs**: Consolidated `v3_1AndAbove`, `v3_2AndAbove`, and `v3_3AndAbove` into a single `v3AndAbove` prop for simpler version management.
- Update `typescript` from `5.9.3` to `6.0.3`

### ✨ Enhancements
- Added `renderError` prop across all components to customize field error rendering.

### 🐞 Bug Fixes
- Fixed MUI error styling being incorrectly applied to helper text when `hideErrorMessage` is enabled.

## [4.0.1](https://github.com/nishkohli96/rhf-mui-components/tree/v4.0.1)

**Released — 30 June, 2026**

### ⚙️ Project & Tooling
- Update `turbo` from `2.9.18` to `2.10.0`.
- Add `prepare-package-json.mjs` script to generate `package.json` for **rhf-mui** build.
- Update `turbo.json` to correct `build`, `run` and `lint` scripts.
- Reorganized project scripts under the root `scripts` directory for better maintainability.
- Updated GitHub Actions to use `scripts/setup.sh` as the single setup entry point for app initialization.
- Add `vercel-ignore-build` script to prevent deploys for `v3` branch or tags on **Vercel**.

### ✨ Enhancements
- Added comprehensive JSDoc documentation for props across all form components.
- Added a "**Disable all fields**" checkbox to each demo form.
- Updated `StyledAutocomplete` to include the `freeSolo` generic.
- Updated `tsconfig.json` for rh-mui package.
- Improved ARIA handling across components by adding hidden-label accessible names, keeping helper/error descriptions, and removing redundant disabled/multiselectable ARIA.
- Update `v3` docs for installing package with the `latest-v3` tag
 
### 🐞 Bug Fixes
- Fixed `FormControlLabel` prop merging order for improved compatibility with `otherFormControlLabelProps`.
- Updated `formLabelProps` and `formHelperTextProps` prop merging order so internally generated `id` values take precedence.
- Passed the `disabled` prop through to the underlying `FormControl` or `FormControlLabel` components.
- Fixed `RHFTagsInput` to correctly apply `ChipProps` and assign an ID to the overflow tag.
- Fixed `RHFSelect` to set `label` value to `undefined` when no value is selected, ensuring correct label behavior.
- Added support for the `disabled` prop on `FormLabel` across all components.
- Fixed MUI picker `slotProps.textField` handling to preserve both object and function forms.
- `RHFColorPicker`: Fixed an issue where `rhfOnChange` was not triggered when the `Hue` component value changed.
- Fixed date/time picker `slotProps.textField` merging so text field props such as `variant`, `color`, and nested `inputProps` are applied correctly while preserving internal ids, error state, blur handling, and accessibility attributes.
- Update `AppRouterCacheProvider` to `v16` for demo app.
- Moved the `customOnChange` interaction warning into each applicable `onValueChange` JSDoc block.


## [4.0.0](https://github.com/nishkohli96/rhf-mui-components/tree/v4.0.0)

**Released — 24 June, 2026**

### ⚙️ Project & Tooling
- Migrated from **Yarn** to **pnpm** for faster and more efficient dependency management.
- Updated `setup.sh` to support the pnpm-based workflow.
- Standardized the Node.js version to **v24.14.0** via `.nvmrc`.
- Change `moduleResolution` from `node` to `bundler`
- Upgraded:
  - **Docusaurus**: `3.7.0` → `3.10.0`
  - **Next.js**: `14` → `16`
  - **Node.js**: `22` → `24`
- Configure examples website URL using environment variables
- Update `actions/checkout` from `v6` to `v7`
- Switched documentation and demo site deployments from **Netlify** to **Vercel**.
- Archived the **v3** [documentation](https://rhf-mui-components.netlify.app/) and [examples](https://rhf-mui-components-examples.netlify.app/) websites. Both Netlify deployments will remain accessible for existing users but will no longer receive updates.
- Add `prepare-package-json.mjs` script to generate `package.json` for **rhf-mui** build.

### 🧩 Core Library Improvements
- Added comprehensive JSDoc comments for component-specific props to improve editor IntelliSense and developer experience.
- Added support for manually triggering `rhfOnChange` through the new `customOnChange` prop across supported components.
- Updated all `onValueChange` callbacks to use a single object-based argument instead of positional parameters, aligning their signature with `customOnChange` for a more consistent developer experience.
- Components now respect the form-level `disabled` option passed through React Hook Form's `useForm` hook.
- Added `hideLabel` to all components to optionally hide the default `FormLabel`.
- Added `customIds` to override the automatically generated IDs used for field, label, helper text, and error message elements.
- Improved accessibility by applying `aria-*` attributes directly to input elements instead of wrapper containers.
- Added external `ref` forwarding support across all components. Component refs are now merged with React Hook Form’s internal field refs to preserve RHF registration and focus behavior.
- Components without built-in labels (such as `RHFSelect`) now respect the global `allLabelsAboveFields` configuration.
- Deprecated the `errorMessage` prop. Validation errors are now handled internally.
- Removed the unused `TrueOrFalse` type.
- Updated version-specific documentation and prop references.
- Switched position of `RHFPasswordInput` and `RHFNumberInput` in docs.
- Removed `validateArray` from option-based components. The intended development-only validation was ineffective because `process.env.NODE_ENV` was consistently resolving to `undefined`, thus defeating the purpose to use it. 
- Updated helper function documentation to show definitions before method signatures.
- Fixed docs website theme toggle visibility and resolved demo website theme flicker where light mode briefly switched to dark on load.

### 🧩 Component-Specific Enhancements
In addition to the core library improvements, several components received new features, customization options, and behavioral improvements.

#### 🧮 RHFNumberInput
Added new input validation and stepping controls:
- `onlyIntegers` — Restricts input to integer values only.
- `nonNegative` — Restricts input to non-negative values.
- `maxDecimalPlaces` — Limits the number of digits allowed after the decimal point.
- `stepAmount` — Controls the increment/decrement amount for keyboard and stepper interactions.

#### 🏷️ RHFTagsInput
Added advanced tag management capabilities:
- `onTagAdd`
- `onTagDelete`
- `onTagPaste`

Additional configuration options:
- `delimiter` — Defines the character used to separate tags.
- `maxTags` — Limits the number of tags that can be added.

#### 📁 RHFFileUploader

##### Added
- Added **drag-and-drop** file upload support (enabled by default and can be disabled using `disableDragAndDrop`).
- Added `existingFiles` for displaying and managing pre-existing server-side files.
- Added `dropZoneProps` for customizing the drag-and-drop area.
- Added `renderExistingFileItem` for custom rendering of existing files.
- Added `existingFileListProps` and `uploadedFileListProps` for customizing file list containers.
- Added `customOnChange` for full control over file validation and form state updates.
- Added support for removing uploaded files via the `renderFileItem` callback.

##### Changed
- `validateFileList` now supports both `FileList` and `File[]`.
- Improved required field validation when `existingFiles` are provided.
- Improved upload count validation by including `existingFiles` in `maxFiles` calculations.
- `onUploadError` now returns detailed file-specific validation errors instead of separate error and file arrays.
- Existing files and newly uploaded files are rendered only when `renderExistingFileItem` and `renderFileItem` are provided, respectively.
- Method signature and return value type for `validateFileList` function 

##### Removed
- `errorMessage` prop as it is now handled internally within the component.

#### 🔘 Select, NativeSelect, CheckboxGroup & RadioGroup
Added support for advanced option customization:
- `getOptionDisabled` — Dynamically disable specific options.
- `renderOptionLabel` — Fully customize option rendering.

#### 🔍 Autocomplete & MultiAutoComplete
##### Added
- Added support for the `freeSolo` prop in `RHFAutocomplete` and `RHFMultiAutocomplete`.
- Added `renderOptionLabel` prop to `RHFMultiAutocomplete` & `RHFMultiAutocompleteObject` for customizing checkbox option labels.

##### Changed
- `autoHighlight` is now configurable via props and defaults to `true`.
- In `RHFMultiAutocomplete`, the "**Select All**" option is automatically hidden when `freeSolo` is enabled.

#### 🌍 RHFCountrySelect
##### Added
- `renderOptionLabel` for customizing the content rendered within each country option.

##### Changed
-  When `valueKey` is provided, selected value(s) are stored using the specified country property.
- When `valueKey` is omitted, selected value(s) are stored as complete country objects.

##### Removed
- Removed the `displayFlagOnSelect` prop.

#### 🔘 CheckboxGroup and RadioGroup
- Updated `rhfOnBlur` behavior to trigger only when focus leaves the entire checkbox/radio group container, rather than when individual options are interacted with.

#### 🗓️ MUI Pickers
- Added **Desktop**, **Mobile**, and **Static** variants for MUI Date & Time pickers, including DatePicker, TimePicker, and DateTimePicker components.
- `rhfOnBlur` is now triggered from `onAccept` to improve validation behavior.

#### 🎨 RHFColorPicker
##### Removed
- `value` prop is no longer needed as the selected color value is now derived automatically from React Hook Form state.

#### ✍️ RHFRichTextEditor
- Added support for forwarding refs to the underlying CKEditor instance.

#### 📱 RHFPhoneInput

##### Added
- Added country search support within the country selection menu, allowing users to quickly find and select a country.
- Added `searchCountryProps` for customizing the country search field and country menu item rendering.

##### Changed
- The field value is now returned as an `RHFPhoneInputValue` object instead of a plain `string`, providing access to additional metadata such as the selected country, dial code, and national phone number.

##### Removed
- `value` prop is no longer needed as it is now derived automatically from React Hook Form state.
- `errorMessage` prop as it is now handled internally within the component.
- Removed `hideDropdown` from `PhoneInputProps`. Use `forceDial` instead to disable selection of country.

### ♿ Accessibility
- Added `aria-checked` and `aria-disabled` attributes to all checkbox and radio controls.
- Improved overall accessibility through more accurate element-level ARIA attributes.

### 📚 Documentation
- Fixed versioned documentation links.
- Fixed example links in older documentation versions to point to the latest examples site.
- Expanded component prop documentation and version-specific API references.

### ⚠️ Error Messages
Improved the clarity and consistency of:
- `generateLabelValueErrMsg`
- `generateDateAdapterErrMsg`
