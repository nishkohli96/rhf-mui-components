import { DocsPage, PropsTable, Callout } from '@/components/docs';
import { componentMetadata, componentProps } from '@/constants';

export const metadata = componentMetadata.RHFCheckboxGroup;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# RHFCheckboxGroup

The **RHFCheckboxGroup** component renders multiple checkboxes allowing
the user to select one or more items from a set. It supports both arrays
and arrays of objects for options, and each [checkbox](https://v7.mui.com/material-ui/react-checkbox/)
can be customized as needed.

## Usage

```tsx
import RHFCheckboxGroup, { RHFCheckboxGroupProps } from '@nish1896/rhf-mui-components/mui/checkbox-group';
```

Example with an array of strings:

```tsx
<RHFCheckboxGroup
  fieldName="toDo"
  control={control}
  label="Tasks to do"
  options={['Read Mails', 'Go for a hike', 'Bring groceries']}
  errorMessage={errors?.toDo?.message}
/>
```

Example with an array of objects:

```tsx
<RHFCheckboxGroup
  fieldName="countries"
  control={control}
  label="Select Countries You've visited"
  options={[
    { code: 'AUS', country: 'Australia' },
    { code: 'IN', country: 'India' },
    { code: 'UAE', country: 'United Arab Emirates' }
  ]}
  labelKey="country"
  valueKey="code"
  errorMessage={errors?.countries?.message}
/>
```

<Callout type="warning">
When using an array of objects for options, both `labelKey` and `valueKey`
are required. If either is missing, an error will be thrown.
</Callout>

## Examples
- [RHFCheckboxGroup](https://rhf-mui-components-examples.vercel.app/checkbox-and-radiogroup)
- [CheckboxGroup](https://v7.mui.com/material-ui/react-checkbox/#formgroup)
- [Complete Form](https://rhf-mui-components-examples.vercel.app/complete-form)

## API
`RHFCheckboxGroupProps` accepts the following props.
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable
  rows={componentProps.RHFCheckboxGroup}
  docsVersion={3}
  muiVersion={7}
  v3AndAbove
/>
