import { DocsPage, PropsTable, Callout } from '@/components/docs';
import { componentMetadata, componentProps } from '@/constants';

export const metadata = componentMetadata.RHFRadioGroup;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# RHFRadioGroup

The **RHFRadioGroup** component renders a group of radio buttons,
allowing users to select one option from a set. It extends the [RadioGroup](https://v6.mui.com/material-ui/react-radio-button/)
component from Material UI, accepts most of its props, and offers
similar customization options. It supports both arrays and
arrays of objects for options.

## Usage

```tsx
import RHFRadioGroup, { RHFRadioGroupProps } from '@nish1896/rhf-mui-components/mui/radio-group';
```

Example with an array of strings:

```tsx
<RHFRadioGroup
  fieldName="favouriteColor"
  control={control}
  options={['Red', 'Blue', 'Orange']}
  errorMessage={errors?.favouriteColor?.message}
/>
```

Example with an array of objects:

```tsx
<RHFRadioGroup
  fieldName="nationality"
  control={control}
  options={[
    { code: 'AUS', country: 'Australia' },
    { code: 'IN', country: 'India' },
    { code: 'UAE', country: 'United Arab Emirates' }
  ]}
  labelKey="country"
  valueKey="code"
  errorMessage={errors?.nationality?.message}
/>
```

<Callout type="warning">
When using an array of objects for options, both `labelKey` and `valueKey`
are required. If either is missing, an error will be thrown.
</Callout>

## API

`RHFRadioGroupProps` extends [RadioGroupProps](https://v6.mui.com/material-ui/api/radio-group/)
and accepts the following additional props.
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable
  rows={componentProps.RHFRadioGroup}
  docsVersion={2}
  muiVersion={6}
/>
