import { DocsPage, PropsTable } from '@/components/docs';
import { componentMetadata, componentPropsV2 } from '@/constants';

export const metadata = componentMetadata.RHFTimePicker;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# RHFTimePicker

**RHFTimePicker** extends the [TimePicker](https://v7.mui.com/x/react-date-pickers/time-picker/)
component to allow users to select time for an event.

## Usage

```tsx
import RHFTimePicker, { RHFTimePickerProps } from '@nish1896/rhf-mui-components/mui-pickers/time';
```

```tsx
<RHFTimePicker
  fieldName="wakeUpTime"
  control={control}
  errorMessage={errors?.wakeUpTime?.message}
/>
```

## API
`RHFTimePickerProps` extends [TimePickerProps](https://v7.mui.com/x/api/date-pickers/time-picker/)
and accepts the following additional props.
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable rows={componentPropsV2.RHFTimePicker} />
