import { DocsPage, PropsTable, Callout } from '@/components/docs';
import { componentMetadata, componentProps } from '@/constants';

export const metadata = componentMetadata.RHFPhoneInput;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# RHFPhoneInput

The **RHFPhoneInput** component is built on top of [react-international-phone](https://www.npmjs.com/package/react-international-phone),
a React library for handling international phone numbers.

It integrates with Material UI by leveraging the [TextField](https://v6.mui.com/material-ui/react-text-field/)
component with the [usePhoneInput](https://react-international-phone.vercel.app/docs/Advanced%20Usage/usePhoneInput)
hook from `react-international-phone`, allowing users to select a country and enter phone number
in the appropriate international format.

<PackageManagerTabs
  id="install-react-international-phone"
  command="react-international-phone"
/>

## Usage

```tsx
import RHFPhoneInput, { RHFPhoneInputProps } from '@nish1896/rhf-mui-components/misc/phone-input';
```

```tsx
<RHFPhoneInput
  fieldName="contactNumber"
  control={control}
  value={getValues("contactNumber")}
  phoneInputProps={{
    defaultCountry: "in",
    preferredCountries: ["in", "us", "au"]
  }}
  errorMessage={errors?.contactNumber?.message}
/>
```

The returned phone value after input is of the type -
- "+91 99887-76655"
- "+1 (765) 232-3423"

<Callout type="note">
  For field validation, make sure to set the minimum length to atleast **6**.
</Callout>

## API
`RHFPhoneInput` extends the props of [TextField](https://v6.mui.com/material-ui/api/text-field/)
and accepts the following additional props.
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable
  rows={componentProps.RHFPhoneInput}
  docsVersion={2}
  muiVersion={6}
  v2
/>
