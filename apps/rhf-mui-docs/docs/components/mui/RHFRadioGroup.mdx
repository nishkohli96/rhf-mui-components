---
sidebar_position: 10
sidebar_label: Radio Group
title: RHFRadioGroup
description: Learn how to use RHFRadioGroup with react-hook-form to create customizable radio button groups. Explore usage examples and API details to enhance your forms.
---

import { RHFRadioGroupPropsTable } from '@site/src/page-components';

# RHFRadioGroup

The **RHFRadioGroup** component renders a group of radio buttons, 
allowing users to select one option from a set. It extends the [RadioGroup](https://mui.com/material-ui/react-radio-button/) 
component from Material-UI, accepting most of its props and offering 
similar customization options. It supports both arrays and 
arrays of objects for options.

### Usage

```js
import RHFRadioGroup, { RHFRadioGroupProps } from '@nish1896/rhf-mui-components/mui/radio-group';
```

Example with an array of strings:

```jsx
<RHFRadioGroup
  fieldName="favouriteColor"
  control={control}
  options={['Red', 'Blue', 'Orange']}
  errorMessage={errors?.favouriteColor?.message}
/>
```

Example with an array of objects:

```jsx
<RHFRadioGroup
  fieldName="nationality"
  control={control}
  options={[
    { code: 'AUS', country: 'Australia' },
    { code: 'IN', country: 'India' },
    { code: 'UAE', country: 'United Arab Emirates' }
  ]}
  labelKey="country"
  valueKey="code"
  errorMessage={errors?.nationality?.message}
/>
```

:::note
When using an array of objects for options, both `labelKey` and `valueKey` 
are required. If either is missing, an error will be thrown.
:::

### Examples
- [RHFRadioGroup](https://rhf-mui-components-examples.netlify.app/checkbox-and-radiogroup)
- [RadioGroup](https://mui.com/material-ui/react-radio-button/)
- [Complete Form](https://rhf-mui-components-examples.netlify.app/complete-form)

### API

`RHFRadioGroupProps` extends [RadioGroupProps](https://mui.com/material-ui/api/radio-group/) 
and accepts the following additional props:

<RHFRadioGroupPropsTable />
