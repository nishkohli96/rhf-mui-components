---
sidebar_position: 1
sidebar_label: Text Field
title: RHFTextField
description: Use RHFTextField with react-hook-form and Material UI to create customizable form fields.
---

import { RHFTextFieldPropsTable } from '@site/src/page-components';
import { DemoLink } from '@site/src/components';
import { ExternalLinks } from '@site/src/constants';

# RHFTextField

Text fields are a core component of any form, allowing users to input text into the UI.
The **RHFTextField** component extends the functionality of [TextField](https://v7.mui.com/material-ui/react-text-field/),
accepting almost all TextField props while providing additional customization options.

## Usage

```js
import RHFTextField, { RHFTextFieldProps } from '@nish1896/rhf-mui-components/mui/textfield';
```

To render a fully functional `RHFTextField`, you only need two or three essential props.

```jsx
<RHFTextField
  fieldName="firstName"
  control={control}
  customOnChange={({ rhfOnChange, newValue }) => {
    /* Override default onChange to capitalize input text */
    rhfOnChange(newValue.toUppercase());
  }}
/>
```

You can extend or reuse the `RHFTextField` component by creating your own wrapper component.

👉 <a href={ExternalLinks.githubExamples.styledTextField} target="_blank" rel="noopener noreferrer">StyledTextField (GitHub)</a>

## Examples
- <DemoLink path="/inputs">RHFTextField</DemoLink>
- <DemoLink path="/customization">Styled RHFTextField</DemoLink>
- [TextField](https://v7.mui.com/material-ui/react-text-field/)
- <DemoLink path="/complete-form">Complete Form</DemoLink>

## API
The `RHFTextFieldProps` interface extends [TextFieldProps](https://v7.mui.com/material-ui/api/text-field/)
and includes the following additional props:

<RHFTextFieldPropsTable
  muiVersion={7}
  v4AndAbove
/>
