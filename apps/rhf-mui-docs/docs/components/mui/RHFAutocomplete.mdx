---
sidebar_position: 8
sidebar_label: Autocomplete
title: RHFAutocomplete
description: Customizable autocomplete component for efficient searching in large datasets, supporting array or object-array options.
---

import { RHFAutocompletePropsTable } from '@site/src/page-components';
import { AvailabilityBanner } from '@site/src/components';
import { ExternalLinks } from '@site/src/constants';

# RHFAutocomplete

<AvailabilityBanner componentName="RHFAutocomplete" version="2.0" />

**RHFAutocomplete** extends [Autocomplete](https://v7.mui.com/material-ui/react-autocomplete/),
designed for searching and selecting an option from large datasets. Similar to
[RHFSelect](./RHFSelect.mdx), it supports selecting single or multiple options and
accepts options as an array of strings or an array of objects. This component is ideal
for use cases requiring dynamic filtering or managing extensive options seamlessly.

:::note
Starting with **v4**, the `freeSolo` prop is now supported in **RHFAutocomplete**, allowing users to enter
values that are not present in the provided options.

When using `freeSolo` with object-based options, custom renderers such as `renderValue`, `renderTags`,
or `renderOptionLabel` should account for both option objects and user-entered string values. A common
approach is:

```ts
typeof value === 'string'
```
to distinguish free-form input from predefined option objects.
:::

## Usage

```js
import RHFAutocomplete, { RHFAutocompleteProps } from '@nish1896/rhf-mui-components/mui/autocomplete';
```

```jsx
<RHFAutocomplete
  fieldName="countriesVisited"
  control={control}
  options={['Azerbaijan', 'Canada', 'India']}
/>
```

For options as an array of objects:

```jsx
<RHFAutocomplete
  fieldName="countriesVisited"
  control={control}
  options={countryList} // imported from RHFCountrySelect
  labelKey="name"
  valueKey="name"
/>
```

:::warning
When using an array of objects for options, both `labelKey` and `valueKey` 
are required. If either is missing, an error will be thrown.
:::

You can extend or reuse the `RHFAutocomplete` component by creating your own wrapper component.

👉 <a href={ExternalLinks.githubExamples.styledAutocomplete} target="_blank" rel="noopener noreferrer">StyledAutocomplete (GitHub)</a>


## Examples
- [RHFAutocomplete](https://rhf-mui-components-examples.netlify.app/autocomplete)
- [Styled Autocomplete](https://rhf-mui-components-examples.netlify.app/customization)
- [Autocomplete Variations](https://v7.mui.com/material-ui/react-autocomplete/#playground)
- [Complete Form](https://rhf-mui-components-examples.netlify.app/complete-form)

## API
The `RHFAutocompleteProps` interface extends [AutocompleteProps](https://v7.mui.com/material-ui/api/autocomplete/) 
and accepts the following additional props:

<RHFAutocompletePropsTable
  muiVersion={7}
  v4AndAbove
/>
