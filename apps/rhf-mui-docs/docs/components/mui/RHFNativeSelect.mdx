---
sidebar_position: 7
sidebar_label: Native Select
title: RHFNativeSelect
description: Discover how to use RHFNativeSelect with react-hook-form for mobile-friendly dropdowns. Explore examples and API details to optimize your React forms.
---

import { RHFNativeSelectPropsTable } from '@site/src/page-components';

# RHFNativeSelect

**RHFNativeSelect** extends [NativeSelect](https://v7.mui.com/material-ui/react-select/#native-select) 
and is ideal for mobile-friendly form designs. Similar to [RHFSelect](./RHFSelect), 
it supports both arrays and arrays of objects for flexible option rendering.

:::tip
If the number of options **exceeds 20**, consider using [RHFAutocomplete](./RHFAutocomplete.mdx)
or [RHFMultiAutocomplete](./RHFMultiAutocomplete.mdx) for better searchability, improved keyboard
navigation, and overall performance.
:::

## Usage

```js
import RHFNativeSelect, { RHFNativeSelectProps } from '@nish1896/rhf-mui-components/mui/native-select';
```

Here’s a basic example with an array of strings:

```jsx
<RHFNativeSelect
  fieldName="color"
  control={control}
  options={['Red', 'Blue', 'Orange']}
/>
```

And with an array of objects:

```jsx
<RHFNativeSelect
  fieldName="country"
  control={control}
  options={[
    { code: 'AUS', country: 'Australia' },
    { code: 'IN', country: 'India' },
    { code: 'UAE', country: 'United Arab Emirates' },
  ]}
  labelKey="country"
  valueKey="code"
/>
```

:::warning
When using an array of objects for options, both `labelKey` and `valueKey` 
are required. If either is missing, an error will be thrown.
:::

## Examples
- [RHFNativeSelect](https://rhf-mui-components-examples.netlify.app/select)
- [Native Select](https://v7.mui.com/material-ui/react-select/#native-select)
- [Complete Form](https://rhf-mui-components-examples.netlify.app/complete-form)

## API
The `RHFNativeSelectProps` interface extends [NativeSelectProps](https://v7.mui.com/material-ui/api/native-select/) 
and accepts the following additional props:

<RHFNativeSelectPropsTable
  muiVersion={7}
  v4AndAbove
/>
