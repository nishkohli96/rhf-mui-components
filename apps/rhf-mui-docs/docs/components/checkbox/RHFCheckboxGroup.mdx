---
sidebar_position: 2
sidebar_label: RHFCheckboxGroup
title: RHFCheckboxGroup
description: Learn how to use RHFCheckboxGroup with react-hook-form to create customizable groups of checkboxes. Explore examples and API details to enhance your forms.
---

import { RHFCheckboxGroupPropsTable } from '@site/src/page-components';

# RHFCheckboxGroup

The `RHFCheckboxGroup` component renders multiple checkboxes allowing
the user to select one or more items from a set. It supports both arrays 
and arrays of objects for options, and each [checkbox](https://mui.com/material-ui/react-checkbox/) 
can be customized as needed.

### Usage

```js
import { RHFCheckboxGroup, RHFCheckboxGroupProps } from '@nish1896/rhf-mui-components';
```

Example with an array of strings:

```jsx
<RHFCheckboxGroup
  fieldName="color"
  control={control}
  label="Agree to Terms & Conditions"
  options={['Red', 'Blue', 'Orange']}
  errorMessage={errors?.color?.message}
/>
```

Example with an array of objects:

```jsx
<RHFCheckboxGroup
  fieldName="countries"
  control={control}
  label="Select Countries You've visited"
  options={[
    { code: 'AUS', country: 'Australia' },
    { code: 'IN', country: 'India' },
    { code: 'UAE', country: 'United Arab Emirates' }
  ]}
  labelKey="country"
  valueKey="code"
  errorMessage={errors?.countries?.message}
/>
```

:::note
When using an array of objects for options, both `labelKey` and `valueKey` 
are required. If either is missing, an error will be thrown.
:::

### Examples
- [RHFCheckboxGroup](../../examples/checkbox-radio)
- [CheckboxGroup](https://mui.com/material-ui/react-checkbox/#formgroup)
- [Complete Form](../../examples/complete-form)

### API
`RHFCheckboxGroupProps` accepts the following props:

<RHFCheckboxGroupPropsTable />
