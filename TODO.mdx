# TODO

- `RHFPhoneInput` rewrite
- `RHFFileUploader` rewrite
- Include `freeSolo` in autocompletes
- Update docusaurus and eslint
- Theme in `demo` app, must preserve even on reload
- Config -> envsForOptionCheck
- hideLabel
- customIds
- rhf-error
- mergeref
- customOnChange
- renderOption, renderOptionLabel
- getOptionDisabled
- maxDecimalPlaces
- stepAmount
- fileuploader -> dragNDrop, handle with default and local value
- phoneInput -> autocomplete for country
- numberinput -> correct decimal additions on ArrowDown and pointers

To fix...date & time pickers selection
customOnChange
onValueChange args
docs card

test file uploader -> fileurl for uploaded files
validation on exceeding files in demo
better file list UI
fix pokemon autocomplete
getOptionKey omit in autocomplete
compare deployed v3 docs
deploy on vercel